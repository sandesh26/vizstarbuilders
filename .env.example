# Example environment variables for SMTP / mail configuration
# Copy this file to .env or set these variables in your hosting environment

# Recipient email address
TO_EMAIL=contact@vizstarbuilders.com

# From address used in message headers
FROM_EMAIL=contact@vizstarbuilders.com
FROM_NAME="Vizstar Builders"

# SMTP settings (PHPMailer) - uncomment and fill in for SMTP sending
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=contact@vizstarbuilders.com
SMTP_PASS=h75Ix1Tw4476@
SMTP_SECURE=ssl

# Optional alternative names
# MAIL_HOST, MAIL_PORT, MAIL_USER, MAIL_PASS, MAIL_SECURE are also supported as synonyms
